# Copy this file to .env and fill in real values.

# Get a free token at https://account.mapbox.com/access-tokens/ (free tier:
# 50,000 map loads/month). Used by CampusMap.jsx for higher-detail tiles —
# the map falls back to plain OpenStreetMap tiles if this is left empty.
VITE_MAPBOX_TOKEN=
